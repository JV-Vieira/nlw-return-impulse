Aula 1
//componente e uma função que retorna HTLM
Coisas importatnes no componente:
Sempre tem que ter o nome maiusculo, e sempre ser o nome da função, para diferinciar componente das funçoes do react

fuction sempre recebe o props
sempre que quiser mostrar uma variavel javaScript coloca uma {}

No TypeScript sempre que passar a tipagem com o INTERFACE NOMEDAINTERFACE {NOMEDAVARIAVEL: OQUE ELA É STRING NUMERO ETC}

instalar o TailWindcss
npm install -D tailwindcss
npx tailwindcss init

utilizar biblioteca phosphor-react

//estado uma variavel, que o react fica observando essa variavel, quando o valor é alterado, o componete é criado novamente com o novo valor